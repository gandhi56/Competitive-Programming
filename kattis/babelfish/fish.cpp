#include <iostream>
#include <cstring>
#include <unordered_map>
#include <sstream>
using namespace std;

int main(){
	string a, b;
	unordered_map<string,string> dict;
	bool read = true;
	while (true){
		string inp;
		cin >> inp;
		istringstream ss(inp);

		if (read){
			int i = 0;
			string k = "", v = "";
			do{
				
			} while (ss);
		}
		else{
			string word;
			cin >> word;
			cout << "searching " << word << endl;
		}
	}

	return 0;
}
