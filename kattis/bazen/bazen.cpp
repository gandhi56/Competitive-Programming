#include <iostream>

using namespace std;

void solve(double x, double y, double len){
	double half_pool = len*len/4;
	
	double xx, yy;
	if (2*x <= len){
		
	}

}

int main(){
	double x, y;
	cin >> x >> y;

	double len = 250.0;

	solve(x, y, len);

	return 0;
}
