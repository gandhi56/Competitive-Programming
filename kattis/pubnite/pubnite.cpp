#include <iostream>
using namespace std;

int xs, ys, ss, ri, rf;
int xa, ya, sa;

int main(){
	cin >> xs >> ys >> ss >> ri >> rf;
	cin >> xa >> ya >> sa;

	int change = 1
	if (xs > 0)

	int d = 0;
	while (1){
		if (ri <= rf)
			break;

		if (xa < (xs - ri))

	}

	return 0;
}
