#include <iostream>
using namespace std;

#define MAXN 1001

int n, m;
int a[MAXN];
int b[MAXN];

int main(){
	int t;
	cin >> t;

	while (t--){
		cin >> n >> m;
		for (int i = 0; i < n; ++i)	cin >> a[i];
		for (int i = 0; i < m; ++i)	cin >> b[i];


		

	}
	return 0;
}
