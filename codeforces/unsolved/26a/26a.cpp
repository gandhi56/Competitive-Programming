#include <bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin >> n;

	vector<int> primes(n+1);
	vector<int> cnt(n+1);
	primes[0] = 0;
	primes[1] = 0;
	for (int p = 2; p <= n; ++p){
		
	}

	return 0;
}
